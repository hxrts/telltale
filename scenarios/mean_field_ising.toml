name = "mean_field_ising"
roles = ["A", "B"]
steps = 1000

[field]
layer = "mean_field"

[field.params]
beta = 0.5
species = ["up", "down"]
initial_state = [0.6, 0.4]
step_size = 0.01

[output]
format = "jsonl"
