import Telltale.Protocol.LocalTypeConvProofs.Part1
import Telltale.Protocol.LocalTypeConvProofs.Part2
import Telltale.Protocol.LocalTypeConvProofs.Part3

/-! # Telltale.Protocol.LocalTypeConvProofs

Conversion Proofs: LocalTypeR ↔ LocalTypeDB Roundtrip.

This module provides proven theorems for the conversion between named (LocalTypeR)
and de Bruijn indexed (LocalTypeDB) representations of local types.

This file uses the canonical definitions from `Telltale.Protocol.LocalTypeConv`,
`LocalTypeR`, and `LocalTypeDB`.
-/
