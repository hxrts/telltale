//! Thin browser shell support for the shared Telltale viewer webapp.
//!
//! The executable entrypoint lives in `main.rs`.
//! This library target exists so the workspace keeps a stable crate boundary
//! for browser-specific packaging and future shell-local helpers.
